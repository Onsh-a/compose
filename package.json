{
  "name": "compose",
  "version": "1.0.0",
  "description": "app to compose",
  "main": "index.js",
  "scripts": {
    "watch": "parcel serve index.html --open --port 3000",
    "build": "parcel index.html --port 3000"
  },
  "author": "Onsha",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.51.0"
  },
  "dependencies": {
    "helpers": "^0.0.6",
    "src": "^1.0.0"
  }
}
